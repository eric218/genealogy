package com.dct.swocean.controller;

import javax.servlet.http.HttpServletRequest;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;

import com.alibaba.fastjson.JSONObject;
import com.dct.swocean.common.ConstantClassField;
import com.dct.swocean.service.TableExistsService;
import com.dct.swocean.util.Response;
import com.dct.swocean.util.ResponseUtlis;

/**
 * 判断表是否存在
 * Craeted by litao on 2018/4/17
 */
@Controller
@RequestMapping("/TableExists")
public class TableExistsController {
	
	@Autowired
	private TableExistsService tableExistsService;
	
	@RequestMapping	("/table/{json}")
	@ResponseBody
	public Response tableExists(HttpServletRequest request,@PathVariable("json")String json){
		JSONObject jsonObj = JSONObject.parseObject(json);
		String table = jsonObj.getString("tablename");
		if (tableExistsService.tableExists(table) != null){
			return ResponseUtlis.success();
		}
		return ResponseUtlis.error(ConstantClassField.DATABASE_TABLE_CODE, ConstantClassField.DATABASE_TABLE_NON_EXIST);
	}
	
}
